//
// Created by Touka on 2023/11/29.
//

#ifndef SCHEDULER_HPP
#define SCHEDULER_HPP
// class Scheduler {
// public:
//   std::vector<TimeSlot>
//   schedule(const std::vector<Schedule> &schedules, const Duration &duration,
//            const Constraints &constraints = Constraints());

// private:
//   TimeSlot choose(const std::vector<TimeSlot> &freeTimes);
//   double getTimeSlotValue(const DateTime &begin, const DateTime &end,
//                           const std::map<DateTime, double> &historyFreq);
//   TimeSlot chooseFromHistory(const std::vector<TimeSlot> &freeTimes,
//                              const std::map<DateTime, double> &historyFreqs,
//                              const Duration &duration,
//                              const std::string &category,
//                              const DateTime &startDate);
//   std::vector<TimeSlot>
//   getFreeSlots(const std::vector<TimeSlot> &busySlots, const DateTime &startISO,
//                const DateTime &endISO,
//                const Duration &minBreakLength = Duration(),
//                const std::map<DateTime, TimeConstraints> &timeConstraints);
// };
#endif //SCHEDULER_HPP
